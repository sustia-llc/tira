[package]
name = "one_many_rs"
version = "0.1.0"
edition = "2021"

[dependencies]
nalgebra = "0.33.2"
rand = { version = "0.9", features = ["os_rng", "thread_rng"] }
rand_distr = "0.5.0"
thiserror = "2.0.12"
serde = { version = "1.0", features = ["derive"] }
plotters = "0.3"
flume = "0.11.1"

[dev-dependencies]
approx = "0.5"
